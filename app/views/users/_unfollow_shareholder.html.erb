<%= form_for(current_user.shareholder_relationships.find_by_shareholder_followed_id(@user),
             html: { method: :delete },
             remote: true) do |f| %>
    <%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>
