<%= form_for(current_user.company_relationships.build(cfollowed_id: @company.id),
             remote: true) do |f| %>
    <div><%= f.hidden_field :cfollowed_id %></div>
    <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>