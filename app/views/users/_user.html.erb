<li>
  <%= gravatar_for user, size: 52 %>
  <%= link_to user.username, user %>
  <% if current_user?(user.username) or current_user.admin?   %>
      | <%= link_to "delete", user, method: :delete,
                    data: { confirm: "You sure?" } %>
  <% end %>
</li>