<%= form_for(current_user.c_relationships.find_by_c_followed_id(@company),
             html: { method: :delete },
             remote: true) do |f| %>
    <%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>
