<% provide(:title, @title) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @company.name %>
      </h1>
    </section>
    <section>

    </section>
  </aside>
  <div class="span8">
    <h3><%= @title %></h3>
    <% if @companies.any? %>
        <ul class="users">
          <%= render @companies %>
        </ul>
        <%= will_paginate @companies, :param_name => :page %>
    <% end %>
  </div>
</div>