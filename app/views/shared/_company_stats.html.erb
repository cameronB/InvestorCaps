<% @user ||= current_user %>
<div class="stats">
  <a href="<%= c_followers_company_path(@company) %>">
    <strong id="followers" class="stat">
      Followed By
      <%= @company.c_followers.count %>
      Shareholders
    </strong>
  </a>
</div>