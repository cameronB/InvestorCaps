<% @user ||= current_user %>
<div class="stats">
  <a href="<%= cfollowers_company_path(@company) %>">
    <strong id="followers" class="stat">
      Followed By
      <%= @company.cfollowers.count %>
      Shareholders
    </strong>
  </a>
</div>